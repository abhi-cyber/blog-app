# Backend -->
<A> FOLDERS

1. cloud :-- Used for the config of cloud storage for the images and data.

2. routers :-- The Router func is called from the express library in 'post.js'. This will help 
               in routing of data between different schemas and the middleware, etc. 

3. models :-- This folder contains the schemas for the database.
              Here the 'featuredPost.js'is made seperately for the featured post as the add 
              them in a seperate folder.

4. db :-- The 'index.js' folder connects the code to the database.

5. controllers :-- This folder contains the functions for creating and saving the different type of posts.
                   More explained in the file.

6. middleware :--           